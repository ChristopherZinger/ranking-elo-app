<!-- <button
	on:click={() => {
		selectedGame = selectedGame?.previousGames?.a || selectedGame;
	}}>parent a</button
>
<button
	on:click={() => {
		selectedGame = selectedGame?.previousGames?.b || selectedGame;
	}}>parent b</button
>
<button
	on:click={() => {
		selectedGame = selectedGame?.nextGames?.winner || selectedGame;
	}}>winner</button
>
<button
	on:click={() => {
		selectedGame = selectedGame?.nextGames?.loser || selectedGame;
	}}>loser</button
> -->
